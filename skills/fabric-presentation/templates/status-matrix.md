---
layout: fabric
---

::header::

# The claim the comparison supports

::default::

<StatusMatrix />

<!--
Claim: the one sentence the filled grid supports. A complete grid claims the comparison is exhaustive, so the claim must be about the whole set, not about one row.

What to say: where each column's data comes from and when it was checked. A matrix of a competitor's capabilities is a claim about someone else, and it goes stale.

Not on the slide (answer if asked): the source per column, the date of the check, the definition behind each "Yes", and the rows deliberately left out.
-->
